apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: http-endpoints
  namespace: monitoring
spec:
  targets:
    staticConfig:
      static:
        - https://argocd.dev-klexify.cloud
        - https://grafana.dev-klexify.cloud
